CREATE TABLE IF NOT EXISTS todo (
                      id VARCHAR PRIMARY KEY,
                      title VARCHAR,
                      description VARCHAR,
                      completed BOOLEAN);